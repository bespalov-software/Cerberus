.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
.venv/

# BoringSSL per-platform build trees (large, reproducible from submodule)
Sources/CCerberus/vendor/build/

# extra/CCerberus.xcframework + extra/headers/ ARE committed — SwiftPM consumers
# need them to be present in the repo when resolving `binaryTarget(path:)`.
# Stripped via `strip -S` they total ~75MB (matches Kalliope's pattern).
